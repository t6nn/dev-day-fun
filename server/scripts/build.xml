<project name="Build All" default="build" basedir=".">
    <description>
        simple example build file
    </description>

    <property name="projectdir" value="/dev/null"/>
    <property name="builddir" location="${projectdir}/che-workspace/out"/>
    <property name="testdir" location="${projectdir}/che-workspace/tests"/>
    <property name="verifydir" location="${projectdir}/verification"/>
    <property name="batsdir" location="${basedir}/bats"/>

    <target name="compile" description="compile the source">
        <mkdir dir="${builddir}"/>
        <javac srcdir="${projectdir}" destdir="${builddir}" includeantruntime="false">
            <include name="Solution.java"/>
        </javac>
    </target>

    <target name="test" description="run tests on the compiled source">
        <exec dir="${testdir}" executable="${batsdir}/bats">
            <arg value="tests.bats"/>
        </exec>
    </target>

    <target name="verify" description="run verification on the compiled source">
        <exec dir="${verifydir}" executable="${batsdir}/bats">
            <arg value="tests.bats"/>
        </exec>
    </target>

    <target name="build" depends="compile,test,verify">

    </target>

</project>